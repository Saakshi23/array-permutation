# array-permutation
This script ensures that every element in a given list is unique. Unlike standard sets that simply remove duplicates, this logic preserves the original array length by incrementing duplicate values.
